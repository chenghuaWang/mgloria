// main-test must include all head files, in order to make
// the compile_command.json for clangd enable.
#include "core.hpp"

int main() {
  LOG_WARN << "Starting tests for MGloria project.\n";
  return 0;
}